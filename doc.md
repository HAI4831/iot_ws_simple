```
tải source code thư viện .zip về https://github.com/Links2004/arduinoWebSockets
sau đó vào sketch import add library zip file để thêm thư viên websocketclient từ đó có thể gọi kết nối tới websocket ssl bằng websocketclient.beginSSL()
```